# 项目记录 / EDS

## 2026-06-06 Kaizen&CI Master Data → 项目跟进 数据迁移

源表：`Kaizen & CI Master Data`（SS_ID `1FXeW4EoofbPkeLOB796ALZBoNsujAwGln-Zaam5OOKA` / Master Data，共37项目）
目标表：`项目总表`（项目跟进后端，A名称/B Leader/C技术员/D状态/E里程碑JSON/F类型）

- 口径：**仅 2026 年 + 仍在进行中**（状态 On Schedule / Behind Schedule），排除 13 个 Done、1 个 Not-started(`周期降低研究`)、2 个已迁移(`热流道自动通断水阀`/`PMC三轴机械手自动润滑`）→ 实迁 **20 个**
- 写入：gsheets MCP `append_values`，INSERT_ROWS，落在 `项目总表!A14:F33`
- 映射规则：
  - 类型 F = `CI`（部门 CI/Kaizen 均归 CI）
  - 状态 D：On/Behind Schedule → `Ongoing`
  - Leader B = `姓名 (邮箱)`，人名→邮箱查 `userID` 权限表；源表 `范超峰` 规范为权限表 `范朝锋`
  - 里程碑 JSON E：源「进展(M列)」非结构化编号文本 → 逐条按 `^\d+[、.]` 拆为节点；节点 `{name:行文本, planned:"NA", actual:"", owner:Leader, ownerEmail, status:"进行中"}`
  - planned 全留 NA（不臆造日期）；节点状态统一「进行中」（文本里的 done/已完成 标记未自动转，待用户在 UI 内逐项标记）
  - 技术员 C、年份/来源/优先级/样机/月度列 等源字段无目标列 → 丢弃
- 生成脚本临时跑在仓库外（`~/migration_gen.js`，已删），未改动任何 GAS 代码，无需 clasp push
- 后续可优化：节点状态/实际完成日期按现实回填；部分长句节点（段落标题被并入）可人工拆分

## 2026-06-03 TLMoC 按钮免密跳转

修改文件：`Navigation_js.html`

### 修改内容
- 新增 `pwd` 从 `sessionStorage` 读取（第 5 行）
- TLMoC 按钮点击时 URL 追加 `?jobNumber=&pwd=&name=` 三个参数（第 201-207 行）
- 配合 Tasklist_MoC 项目新增的 `tryAutoLogin()` 函数，实现跨项目免密登录

## 2026-06-04 Inspection2.0 点检记录合并：6 Sheet → 1

修改文件：`Code.js`

### 修改内容
- 新增 `USE_UNIFIED_INSPECTION_SHEET` 开关（紧急回滚设 false）
- `getData_PointCheck()`：Sheet 名从 `process + "-" + workshop` 改为 `"InspectionRecords"` + B/C 列过滤
- `getData_PointCheck_Inspection2()`：INJ-TB1/TB2 硬编码、TF/PK 动态表名、遍历 sheet 构建 allHistoricalRecords 三处统一改为读 InspectionRecords + 按工序过滤
- `upload_PointCheck()` / `submitInspectionResults()`：写入目标从动态 `process + "-" + workshop` 改为统一 `"InspectionRecords"`
- 新增 `migrateToInspectionRecords()` 一次性迁移函数 + `countInspectionRows()` 验证函数

### 原因
- 6 个 Sheet（INJ-TB1/TB2、TF-TB1/TB2、PK-TB1/TB2）列结构完全一致，B/C 列已含车间/工序
- 合并后查询更简单、新增工序/车间无需新建 Sheet、统计不需跨表聚合
- 减少 API 调用次数（原来 2-6 次变为 1 次）

## 2026-06-05 项目跟进模块后端去冗余 + 2.0 双类型

修改文件：`Code.js` / `ProjectTracking.html` / `ProjectTracking-js.html` / `项目总表`(后端表)

### A. 去冗余（已 commit + clasp push，V20260605.02/03）
- 里程碑从「固定列 D–Q + JSON 双写」收敛为单一数据源（JSON）
- 数据迁移 10 项目 → JSON，物理删除冗余 14 列 D–Q（Status/JSON 左移至 D/E）
- 抽 `parseMilestonesJSON_` helper 消除 4 处重复解析
- 修 2 个隐藏 bug：`handleApprovalAction` 审批改写 JSON、`requestPlannedDateDelay` 按项目自身 JSON 索引
- 删死代码 `sendPlannedDateChangeNotification` + 已完成的迁移函数

### B. 2.0 双类型录入+可视化
- 需求文档：`需求文档_项目跟进2.0.md`
- 后端：新增 `PROJECT_TYPE_COL=5`(F列)；`getProjectTrackingData` 返回 type；`addProject` 写 type + 节点 owner/ownerEmail；`updateProjectTracking` milestonesReplace 透传 owner
- 后端表：F 列「类型/Type」，历史 10 行回填「标准」
- 前端录入：Add 弹窗加项目类型选择，标准走里程碑模板 / CI 走 follow-up 行（事项+责任人 select2 选人+计划完成）
- 前端可视化：主表加类型徽章列 + 类型筛选；展开按类型自适应（标准=甘特时间线 / CI=责任人清单表 `renderCIChecklist`）；更新弹窗 CI 带责任人列
- 范围：仅录入+可视化；定时 Gmail 提醒不做，仅结构预留 owner/email

### 待办
- 生产部署 @432 仍旧版，与新表结构不兼容；用户通知后再发版（exec URL 不变）
