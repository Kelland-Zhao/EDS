20251121[text](PM_Task_1.0.html)
- PM 开始时自动链接后端当月PM工单数据库
- 优先使用【设备号】匹配当月工单
- 其次自主选择当月工单，支持模糊查找

20251122 [text](Inspection2.0.html)
- 【基础参数】增加车间/工序联动功能
- 【任务参数】增加点检人类型/ 任务类型联动功能
- 车间/ 工序/ 点检人类型/ 任务类型 保留手动选择功能
- 上述联动功能主数据来自于https://docs.google.com/spreadsheets/d/1RQql-PrcBWiAQNeg7hQKcocpllSUMRhT5XPrDTVWoBY/edit?gid=441966985#gid=441966985，sheetname = MachineList

20251123[text](Inspection2.0.html)
- 车间/ 工序/ 点检人类型/ 任务类型/ 检查频次 实现联动功能
- 车间/ 工序/ 点检人类型/ 任务类型 联动主数据来自于https://docs.google.com/spreadsheets/d/1RQql-PrcBWiAQNeg7hQKcocpllSUMRhT5XPrDTVWoBY/edit?gid=441966985#gid=441966985，sheetname = MachineList
- 检查频次 联动主数据来自于任务清单第一条的frequency
- 机台号选项来源：1. 非海天机 2. All_RunTime_PointCheck 这个表里面当班触发的机台； 3. All_RunTime_PointCheck 表里面”未作“的机台

20251125[text](Inspection2.0.html)
- 弹窗，选择工序，加载后端数据
- 填写说明

20251126
- [将‘保养工单号’的选择从“描述”改为“工单描述”](PM_Task_1.0.html)

20260105
- 后台增加Workcenter_List_Others
- 具体实现
    从同一个 Spreadsheet ID：1RQql-PrcBWiAQNeg7hQKcocpllSUMRhT5XPrDTVWoBY
    Sheet 名称：Workcenter_List_Others
    读取第2行开始，共3列（Workcenter、Machine_Type、Process）
    筛选 Process：
    当 process === "INJ" 时，筛选 Process = "IM" 的记录
    与 MachineList 的 D 列取交集（复用现有的 nimWorkcenterSet）
    排除当前周次已检查的机台号（复用现有的 excludedWorkcenters）
    合并到 nimWorkcenters（在现有 nimWorkcenters 构建后合并）
- "如果选项唯一，自动选中" 功能删除

20260116[text](Inspection-js_2.0.html)(Code.js)
- 提交检查结果成功后，不再刷新页面
- 保留点检人类型、任务类型、班次、工号、姓名、检查日期
- 清空机台号、车间、工序、检查频次、点检任务表格
- 用户可直接选择新的机台号继续点检
- 修正周次计算逻辑（getYearWeek函数）
- 周的定义：周一至周日为一周
- 第一周规则：第一周从1月1日所在周的周一开始，到该周的周日结束（无论1月1日是周几）
- 计算逻辑：找到当前日期所在周的周一和1月1日所在周的周一，计算周数差+1
- 示例：2026年1月1日（周四）所在周为2026W01，1月5日-1月11日为2026W02，1月12日-1月18日为2026W03
- 删除所有调试日志输出（console.log、console.warn、console.error），保留错误过滤功能代码
- 修复INJ工序机台号排除逻辑问题
- 问题：当后端返回的workcenter_list为空数组（已排除所有已检查机台号）时，前端会进入else分支，从machine_info、current_PointCheckInfo、history_PointCheckInfo等数据源重新构建机台号列表，导致已排除的机台号重新出现在下拉框中
- 解决方案：对于INJ工序，即使workcenter_list为空数组，也使用它（空数组），不从其他数据源重新构建
- 修改逻辑：在loadData函数中保存process到window.currentProcess，在setupSelectOptions中判断如果是INJ工序且workcenter_list存在（即使为空），直接使用workcenter_list
- 添加调试日志：输出NIM机台号排除逻辑、IM机台号信息、最终机台号下拉框数据，用于追踪排除逻辑执行情况

20260206[text](Inspection-js_2.0.html)(Code.js)(Inspection2.0.html)
- 重构任务类型数据源逻辑
- 任务类型选项改为从 MachineList 的第三列（Machine_Type）获取，不再使用 Tasklist_history
- 检查频次和点检清单详情功能保持使用 Tasklist_history
- 检查频次显示改为动态获取：从 Tasklist_history 中获取匹配任务类型的第一条记录的 Frequency 字段值
- 删除 Inspection2.0.html 中检查频次的硬编码 option 标签，仅保留 placeholder
- 增加检查频次错误处理：显示具体原因（无对应任务清单、任务清单不是数组、数组为空、缺少Frequency属性、JSON解析失败等）
- 重构 INJ 工序机台号获取逻辑（NIM + IM 双轨制）
- 后端数据准备（Code.js）：
  - NIM机台号原始列表（nimWorkcenters_raw）：
    - 数据源A：Workcenter_List_IM（从第1行开始读取A-B列，筛选机型=ENG/FCS/6AX）
    - 数据源B：Workcenter_List_Others（从第2行开始读取A-C列，筛选Process=IM）
    - 两个数据源的机台号均需与 MachineList 的 D列（机台号）取交集
    - 合并后传给前端，不做排除处理
  - 当周已检查组合（excludedCombinations）：
    - 从 INJ-TB1 和 INJ-TB2 读取（第2行开始，A-I列）
    - A列提取周次信息（正则匹配 YYYYWXX 格式）
    - 匹配当前周次的记录，提取 D列（任务类型）和 I列（机台号）
    - 存储为对象数组：{ workcenter: 机台号, machineType: 任务类型 }
  - PointCheckInfo 数据（pointCheckInfo）：
    - 筛选"是否已做"≠"已做"的记录
    - 按工序筛选（INJ工序同时匹配INJ和IM）
    - 完整对象数组传递给前端
- 前端动态过滤（Inspection-js_2.0.html）：
  - 用户选择任务类型后触发过滤
  - NIM机台号过滤：
    - 从 MachineList 筛选该任务类型对应的所有机台号
    - 与 NIM原始列表（nimWorkcenters_raw）取交集
    - 排除当周已检查的"该任务类型+机台号"组合
    - 格式化为选项（无红色备注）
  - IM机台号过滤：
    - 从 PointCheckInfo 筛选该任务类型的记录
    - 筛选"是否已做"≠"已做"的记录（包括空值和"未做"）
    - 提取机台号字段
    - 如果状态="未做"，添加红色备注（格式：年周_开始日期_开始班次）
  - 合并与去重：
    - 合并 NIM 和 IM 机台号数组
    - 基于机台号ID去重（使用 Map 数据结构）
    - 更新机台号下拉框
- 修复后端变量作用域问题
- 问题：excludedCombinations 在 if (process === "INJ") 块内部多次定义，导致在 info 对象中无法正确访问
- 解决：将 nimWorkcenters_raw 和 excludedCombinations 变量定义移到 if 块外部，确保作用域正确
- 添加调试日志用于问题排查
- 后端日志（Code.js）：
  - PointCheckInfo 原始数据、过滤后数据、按任务类型分组统计
  - 按工序筛选后的数据和任务类型统计
  - 返回前确认各数据集数量
- 前端日志（Inspection-js_2.0.html）：
  - PointCheckInfo 原始数据和筛选过程
  - 按任务类型筛选结果、按状态筛选结果
  - IM机台号最终结果（含红色备注信息）

20260207[text](Inspection-js_2.0.html)
- 机台号变化时的车间和工序筛选逻辑优化
- 添加前端日志：显示机台号变化时筛选出的可选车间和工序
- 实现车间和工序的自动填入功能：当筛选结果唯一时自动选中并更新背景色
- 修复机台号选择框显示问题：选择任务类型后保持placeholder状态而非自动显示第一个选项
- 统一所有机台号Select2配置，添加显式placeholder选项，确保正确显示"机台号/ Workcenter"
- 检查频次获取到有效值时自动更新背景色为绿色，与其他已选择字段保持视觉一致性
- 优化重置和错误状态下的背景色处理
- 提交成功后的优化处理：清空任务类型、机台号、车间、工序、检查频次
- 保留点检人类型、班次、工号、姓名、检查日期
- 从全局机台号选项中移除已提交的机台号（globalWorkcenter、globalNIMWorkcenters）
- 更新 globalPointCheckInfo 中对应机台号的状态为"已做"
- 用户可直接选择新的任务类型和机台号继续点检，已提交的机台号不会再出现
- 删除所有前端调试日志（console.log）

20260208[text](Navigation.html)(Navigation_js.html)
- 导航页点检按钮功能优化
- 点击"点检"按钮后弹出选择模态框（参考故障报告实现方式）
- 添加两个选项：
  - 点检执行：跳转到 Inspection2.0.html（内部页面）
  - 点检报告：跳转到 Domo 报表 https://colpal.domo.com/page/1025333512/view/114391177
- 不需要权限控制，所有用户均可访问

20260208[text](Code.js)(Inspection-js_2.0.html)
- 为 TF（植磨毛）和 PK（包装）工序添加当周已检查机台号排除逻辑
- 后端修改（Code.js）：
  - 从 TF-TB1/TF-TB2 或 PK-TB1/PK-TB2 读取当周已检查记录
  - 提取当周的"机台号+任务类型"组合，存储到 excludedCombinations
  - 数据结构与 INJ 工序保持一致
- 前端修改（Inspection-js_2.0.html）：
  - 更新 globalExcludedCombinations 注释：现在用于所有工序（INJ/TF/PK）
  - 在任务类型变化时，TF/PK 工序应用排除逻辑
  - 从 MachineList 获取机台号后，排除当周该任务类型已检查的机台号
- 三个工序对比：
  - INJ：排除当周已检查 + 显示 IM 未完成任务红色备注 + NIM 双轨制
  - TF/PK：仅排除当周已检查 + 仅从 MachineList 获取机台号

20260209[text](Inspection-js_2.0.html)
- INJ工序多周次未做记录显示优化
- 问题：同一个机台号在不同周次都有未做记录时，只显示第一个周次的红色备注，其他周次被去重逻辑过滤
- 解决方案：
  - 修改 IM 机台号数据结构：为每条记录添加 uniqueKey 字段（机台号_年周）
  - 修改 NIM 机台号数据结构：添加 uniqueKey 字段（机台号本身）
  - 修改去重逻辑：基于 uniqueKey 而非 id 进行去重
  - 效果：同一机台号在不同周次的未做记录会作为不同选项显示，用红色备注区分
- 机台号选择逻辑优化：
  - 在机台号 change 事件中保存完整选项信息（uniqueKey 和 secondInfo）到 DOM data 属性
  - 确保后续处理能够精确识别用户选择的是哪个周次的记录
- 提交后移除逻辑优化（removeWorkcenterFromOptions 函数）：
  - 使用保存的 uniqueKey 和 secondInfo 精确匹配要移除的记录
  - 对于有 secondInfo 的记录：仅标记匹配特定周次的记录为"已做"
  - 对于无 secondInfo 的记录：标记所有该机台号的记录为"已做"（保持 NIM 的原有逻辑）
  - 避免误将其他周次的未做记录也标记为已做
- Bug修复：
  - 问题：点检人类型变化时，机台号被清空并触发 change 事件，但此时 Select2 可能未初始化，导致调用 select2('data') 报错
  - 解决：在机台号 change 事件中添加条件检查：只在机台号有值且已初始化为 Select2 时才获取 select2 数据
  - 使用 hasClass('select2-hidden-accessible') 判断是否已初始化
  - 修复后点检人类型选择不再影响任务类型的正常显示
- INJ 工序 IM 机台号交叉验证排除逻辑
- 需求背景：
  - PointCheckInfo 中显示"未做"或状态为空的记录
  - 但实际上可能已在 INJ-TB1/TB2 中提交过（数据不一致）
  - 需要防止用户重复提交
- 实现逻辑：
  - 仅针对 INJ（注塑）工序，TF/PK 工序不受影响
  - 在处理 IM 机台号时，增加交叉验证过滤步骤
  - 对于 PointCheckInfo 中"是否已做"字段为"未做"或"空字符串"的记录
  - 检查其"机台号 + 任务类型"组合是否存在于 globalExcludedCombinations 中
  - 如果存在（即该周在 TB1/TB2 中已有提交记录），则排除该记录，不在机台号选择框中显示
  - 只排除特定年周的记录，同一机台号的其他年周记录不受影响
- 效果：
  - 避免数据不一致导致的重复提交
  - 用户看到的机台号选项更准确，只显示真正需要检查的机台

20260211[text](Code.js)(Shift_1.0-js.html)
- Shift_1.0 交接班系统 - 故障报告字段自动化
- 需求背景：根据维修时间自动判断"是否需要填写故障报告"字段，避免手动判断疏漏
- 实现逻辑：
  - 后端 Code.js submitFailure：维修时间 >= 240分钟 → "是否需要填写故障报告" = "是"；< 240分钟 → "否"
  - 前端 Shift_1.0-js.html submitFailure：同步相同判断逻辑，表格渲染时字段值正确显示
- 修改位置：Code.js 第3279-3285行；Shift_1.0-js.html 第146-152行
- 效果：自动化判断、前后端数据同步、标准化故障报告填写流程

20260212[text](机械加工模块-需求说明_V1.1.md)
- 机械加工模块业务需求说明整理与迭代（V1.2 内容）
- 新增完整需求说明文档 机械加工模块-需求说明_V1.1.md（文档内版本号 V1.2），含：模块目的、角色与职责、工单类型、流程图（Mermaid）、工单状态、业务规则、数据字段、系统对接、权限、通知、待定事项
- 流程与规则补充：
  - 第一次加工的备件增加测试验收环节，由需求发起者执行；验收通过后直接交付现场使用（不经过备件房），该备件再次加工时才经备件房领用
  - 成本估算提前至审核通过后，作为申请备件信息的一环（与需求发起人在备件系统申请同步），由上级工程师执行；加工中仅保留修正成本
  - 安全库存为 0 但需加工的备件：确定采用方案 A（备件系统手动触发）；备件系统已支持手动触发采购申请，通过数据对接推送至机械加工模块
- 工单类型扩充：
  - 新增「备件加工-手动触发」：由备件系统手动发起采购申请后推送，无需审核、备件视为已就绪、图纸按备件编码带出，流程与备件房补充安全库存一致
  - 工单类型枚举：常规需求 / 备件房补充安全库存 / 备件加工-手动触发
- 全文同步：审核规则、图纸来源、完成后处理、测试验收适用范围、对接场景、工单主表字段与对接示例均补充或引用新工单类型

20260214[text](Shift_1.0.html)(Shift_1.0-js.html)(Code.js)(home_new_1.0.html)
- Shift_1.0 交接班系统 - 用户界面优化与数据源升级
- 需求背景：提供清晰填写说明与处理规则、优化表格显示、明确重大故障判定标准、机台号与三工序计划Matrix联动
- 实现内容：
  - 故障记录说明区域（Shift_1.0.html 第104-116行）：需填写本班欠产大于半小时的机台、重点质量问题跟进（注塑工艺参数异常/植磨毛无金属丝），Bootstrap alert-info 中英双语
  - 故障历史说明区域（第323-332行）：重大故障标准 IM>240分钟/TF>120分钟/PK>60分钟，重大故障需每日SDM讨论，Bootstrap alert-info 中英双语
  - 表格列格式（Shift_1.0-js.html 第381-414行）："是否需要填写故障报告""是否跟随"列 Badge 格式（是=红/否=绿），列居中对齐
  - 机台号数据源（Code.js 第3182-3200行，Shift_1.0-js.html 第215-243行）：从 `2. Active Cell` 改为 `Workcenter_V202602`（文件ID 1bYKTK5a63yJWRHzM_UPP6b4hwF67eZKEM5dCKLWR59U），A/B/C列机台号/车间/工序；前端按登录工序过滤、INJ/IM 互通、选机台号后自动填车间
  - 数据源说明（Shift_1.0.html 第118-125行）：机台号与三工序计划Matrix联动 Workcenter_V202602、故障代码与OE联动 errorCode 2.0，alert-warning 中英双语
  - 登录界面（home_new_1.0.html 第47/54行）：工号与密码输入框灰色底色 #e0e0e0
- 效果：填写要求与数据源说明清晰、机台号自动联动与过滤、重大故障标准明确、表格可读性提升、登录框对比度改善

- Shift_1.0 故障处理区域 - 后端配置驱动动态渲染
- 需求背景：故障处理项从后端配置表读取，不再硬编码；Remark=跟从 保持热流道跟从双输入框交互
- 实现内容：
  - 后端 Code.js：新增 getFailureProcessConfigData()，数据源 Spreadsheet 10Fnrqc1AUiPqOi-b2UsKgR-Ww-BNdIla_HB_HjVdI0w Sheet `故障处理选项设置`，返回 Description/Process/Placeholder/Remark
  - 前端 Shift_1.0.html：故障处理区域改为动态容器 #failureProcessContainer
  - 前端 Shift_1.0-js.html：加载时获取配置、按 sessionStorage.Process 筛选（INJ 映射 IM）、Remark=跟从 时渲染热流道跟从穴号结构；处理过程按动态条目拼接提交、跟从写入是否跟随、必填校验与事件委托兼容动态节点
  - Shift_1.0.html 数据源说明新增：故障处理条目来自各工序 `故障处理选项设置` sheet（中英双语）
- 修改文件：Code.js 新增接口；Shift_1.0.html 动态容器；Shift_1.0-js.html 动态渲染/筛选/提交/校验

- Shift_1.0 故障记录字段与故障历史展示增强
- 故障报告字段判定逻辑调整（前后端一致）：
  - IM：维修时间 < 240min → “否”
  - TF：维修时间 < 120min → “否”
  - PK：维修时间 < 60min  → “否”
  - 其他情况 → 空值（INJ 按 IM 口径）
- 故障历史表格列显示优化：
  - 基于 sessionStorage.Process：仅注塑（INJ/IM）显示“是否跟随”列，TF/PK 隐藏
  - 新增字段写入后修复 DataTable 列索引：跟进按钮列改为动态 targets=head.length，避免新增列后错位
- 故障记录新增输入字段（并写入 Sheet 指定列）：
  - “直接原因”写入 W 列；“建议措施”写入 X 列（前端 submitFailure 传递 obj['直接原因']/obj['建议措施']，后端 appendRow 追加两列）
  - 提交成功后清空上述输入框
- 故障历史“问题描述”列重命名与内容重构：
  - 列头显示改为“故障信息”
  - 内容按三段展示：【问题描述】【直接原因】【建议措施】（换行显示）
- 故障记录填写说明补充：
  - 增加“故障处理过程”操作步骤与提示：默认选“是”、选择“是”后续输入不可为空，否则无法提交
- (FailureReport_View-js.html)(FailureReport_Progress-js.html)(Code.js)
- 故障报告相关页面 - 基于 Process 的 Tab 显示
- 需求：故障报告上传、查看、进度三个页面的表格/列表与 FailureReport_Manage 一致，按当前登录工序决定默认显示的 Tab
- 实现逻辑（与 FailureReport_Manage 一致）：
  - 读取 sessionStorage.Process（兜底 sessionStorage.pmProcess），INJ 映射为 IM
  - 三道工序 Tab（IM/TF/PK）均保留可见，不隐藏
  - 仅将当前工序对应的 Tab 按钮与内容块 prepend 到最前，并用 Bootstrap Tab 激活
- 修改文件：
  - FailureReport_Upload-js.html：在 $(document).ready 开头增加 setDefaultProcessTabBySession()
  - FailureReport_View-js.html：同上，在 initPage() 之前执行
  - FailureReport_Progress-js.html：同上，在 Swal 与数据加载之前执行
- 故障报告上传文件保存地址说明
- 三工序（IM/TF/PK）共用一个 Google Drive 保存地址
- Code.js uploadFailureReportAttachment 使用固定 folderId：1mMKiMFOzbpqB_V2iIQcIqF2o4ZyRRcNL
- 通过标准化文件名（故障报告编号_车间_工序 + 扩展名）区分不同报告，未按工序分文件夹

20260215[text](Shift_1.0-js.html)(Shift_1.0.html)
- Shift_1.0 交接班系统 - 故障历史表格合并显示与跟进模态框优化
- 需求：同一故障的跟进记录（编号前缀相同）在故障历史表格中合并为一行显示
- 实现内容：
  - 按编号合并逻辑（mergeFailureHistoryRows）：
    - 按"编号"中第一个"-"之前的部分分组
    - 每组按"-"后数字升序排序，取数字最大的条目为基准行
    - 状态、机台号等单值列：取数字最大条目的值
    - 故障信息、处理过程、维修时间、维修人、故障代码、提交日期、是否跟随：按数字大到小排列（多段显示）
  - 维修时间增强：
    - 在单元格最下方显示【合计维修时间】：所有条目维修时间之和
    - 使用 flex 布局使合计在单元格底部居左对齐
  - 子条目间隔：每个子条目增加 margin-bottom: 0.75em 便于视觉区分
  - 组内对齐（alignMergedGroupRows）：
    - 每条跟进包裹在 <div class="group-row" data-group-index="序号"> 中
    - 同一组在各列的 group-row 统一 min-height，使该组首行在各列水平对齐
    - 在 drawCallback 中调用，每次表格绘制后执行对齐
  - 跟进按钮：合并行只显示一个跟进按钮
  - DataTable 重复初始化修复：刷新前先检查并 destroy() 已有实例
- 跟进模态框动态化：
  - 故障处理区域改为与主页面一致，由后端"故障处理选项设置"按当前工序动态生成
  - 新增 renderFailureProcessForModal 和 globalFailureProcessItemsModal
  - 模态框中增加"直接原因"和"建议措施"输入框，与主页面一致
  - 主页面和模态框均校验直接原因、建议措施为必填
- 故障处理提交优化：
  - 处理过程写入后端时，标签只保留中文部分（按"/"前截断）
  - 选"否"的项也写入标题，格式：【标题】：（冒号后为空）
- 提交成功流程：
  - 主页面提交：先关弹窗、显示"保存成功"，再 addNewRow 和清空表单
  - 模态框提交：先刷新全表数据，表格渲染完成后再关弹窗、显示"保存成功"
- 遗留问题：
  - 模态框提交后前端表格数据更新较慢（需要等待全表拉取与重绘），"数据上传中"弹窗显示时间较长
  - 待优化方案：乐观更新（前端直接更新当前行不拉全表）或增量接口（只返回本组数据）
